# forked from ~/.tmuxinator/usual.yml

name: hermes
root: ~/code/notreon/hermes

windows:
  - coding:
      layout: 'main-horizontal'
      panes:
        - pipenv shell
        - pipenv shell -- python -m pytest --cov=hermes --cov-report term-missing
