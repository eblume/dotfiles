# ~/.ssh/config
#
# Reminder to myself: if I'm looking for where my ssh key is stored and specified, it's set up in ~/.zshrc by retrieving
# it from 1password and setting it in ssh-agent.

Include /Users/erich/.colima/ssh_config


# Disable password authentication - it's never what I want
PasswordAuthentication no


## Tailscale username mapping
Host ringtail
  User erich
